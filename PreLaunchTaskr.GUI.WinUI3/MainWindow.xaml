<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="PreLaunchTaskr.GUI.WinUI3.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:PreLaunchTaskr.GUI.WinUI3"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:my="using:PreLaunchTaskr.GUI.WinUI3.Controls"
    xmlns:vm="using:PreLaunchTaskr.GUI.WinUI3.ViewModels.ItemModels"
    Title="PreLaunchTaskr.GUI.WinUI3"
    Closed="Window_Closed"
    mc:Ignorable="d">

    <!--  Windows 10 不支持 Mica，将会在 MainWindow 代码中判断并使用不同材质。  -->
    <!--<Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>-->

    <Frame
        x:Name="ContentFrame"
        x:FieldModifier="private"
        IsNavigationStackEnabled="False"
        Loaded="Frame_Loaded" />
</Window>
